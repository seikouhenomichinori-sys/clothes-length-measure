# 服の着丈自動計測アプリ

## 概要

服を平置きした状態でカメラ撮影し、ID-1規格カード(クレジットカード/免許証サイズ, 85.60×53.98mm)を長さの基準にして、服の着丈(縦方向の長さ)を自動計測するWebアプリ。

### 利用フロー
1. アプリを開くとカメラが起動し、左下にカードのガイド枠(ID-1のアスペクト比固定)が表示される
2. ユーザーがガイド枠にカードを合わせ、画面中央に測定対象の服を配置する
3. シャッターボタン(画面下部中央のUI要素)を押して撮影する
4. 服の着丈を自動計測し、画面に表示する
5. 撮影画像を自動トリミングする(カード部分を含む左下領域を画角から除く)
6. トリミング後の画像を保存するかどうかユーザーに確認する

### 画面レイアウト
- 左上: メニューバー
- 中央〜全体: カメラのプレビュー映像
- 左下: カード用ガイド枠(固定位置・固定サイズ、ID-1アスペクト比)
- 下部中央: シャッターボタン

### 撮影条件の前提(精度に影響するため厳守)
- 背景は無地を推奨
- カメラは対象平面(服・カードが置かれた面)に対してできるだけ水平・垂直に構える(斜めに構えると画面奥/手前でmm/px比がズレて誤差が出るため)。UI上で注意喚起する。

## 技術スタック

- **ビルド/フレームワーク**: Vite + React + TypeScript(SSR不要のためSPA構成)
- **カメラ**: ブラウザ標準の `getUserMedia`。静止画取得は `canvas.drawImage(video, ...)` 方式(`ImageCapture` APIはSafari未対応のため不使用)
- **画像処理(着丈計測)**: Canvas 2D API による自前実装から開始
  - 背景色サンプリング → 色距離による二値化 → 行スキャンで前景ピクセルの上端・下端Y座標を特定
  - OpenCV.js(WebAssembly版)は精度不足が判明した場合にのみ後日導入を検討する(初期導入はしない。8-10MBのWASMバンドルによる初期ロード増を避けるため)
- **データ保存**: IndexedDB(MVPではバックエンドなし。ラッパーライブラリの利用可否は実装時に判断)

### 採用しない/保留する機能(MVPスコープ外)
- 自動検出結果に対する手動補正UI(上端・下端のドラッグ修正)は今回のMVPには含めない。自動検出の精度検証後、必要なら別途追加を検討する。
- カードの自動検出(矩形検出等の画像認識)は行わない。ユーザーが画面上の固定ガイド枠に物理的にカードを合わせることでスケール基準を得る。

## 座標系に関する注意(実装上の要注意ポイント)

ガイド枠はCSSピクセル座標(プレビュー表示上の座標)で描画されるが、実際に撮影される画像データは `<video>` のネイティブ解像度のピクセル座標を持つ。`object-fit` 等でプレビュー表示にクロップ・拡縮が入るとこの2つの座標系はズレるため、mm/px換算スケールを算出する際は必ずプレビューの実表示サイズ・オフセットとネイティブ解像度の比率を計算した上でガイド枠座標をネイティブピクセル座標へ変換すること。

## ディレクトリ構成(予定)

```
src/
  components/     # UIコンポーネント(CameraView, GuideFrame, ShutterButton, MenuBar など)
  hooks/          # カメラ制御・撮影処理などのReact hooks
  lib/            # 純粋関数群(スケール計算、輪郭/端点検出、トリミング処理) - Reactに依存しない
  storage/        # IndexedDBまわりの永続化処理
  types/          # 共有型定義
  App.tsx
  main.tsx
public/
index.html
```

## コーディング規約

- TypeScript strict モードを使用する
- 関数コンポーネント + hooks で統一する(クラスコンポーネント不使用)
- 計測ロジック(`lib/` 配下)はReactに依存しない純粋関数として実装し、単体でテスト可能にする
- コンポーネント名・ファイル名はPascalCase、hooks/utilはcamelCase
- コメントは「なぜそうしたか」が非自明な箇所にのみ最小限記述する。処理内容の説明コメントは書かない
- 過剰な抽象化・将来を見越した汎用化は行わない。今必要な実装のみ行う

## 開発の進め方

以下の順で小さく実装し、各ステップごとに動作確認とユーザー確認を取ってから次に進む。

1. カメラ起動 + ガイド枠 + シャッターボタンのUI(画像処理なし)
2. 撮影して静止画を取得する処理
3. ガイド枠の座標からmm/px換算スケールを算出する処理
4. 服の輪郭検出(Canvas APIベース)→ 着丈の自動計算
5. 自動トリミングと保存確認フロー

大きな設計判断が必要な場面では、実装前に必ずプランを提示し合意を得る。
